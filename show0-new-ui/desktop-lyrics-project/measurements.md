# Desktop Lyrics UI Measurements

Source reference: user-provided desktop lyric screenshots on 2026-05-18.

## Main Regions

- Green desktop lyric frame: `506 x 327 px`.
- Electron window target: `514 x 331 px`, so the visible desktop lyric frame stays `506 x 327 px` with the first icon row top aligned to the window top edge.
- Transparent padding: left `4 px`, right `4 px`, bottom `4 px`, top `0 px`.
- Yellow lyric icon area: about `490 x 34 px`, centered at the top.
- Red lyric display area: starts under the yellow toolbar at about `31 px` from the frame top and fills the remaining lower area.
- First icon row: `30 px`, with icons.
- Second icon row: `26 px`, reserved empty row with no icons.
- Gap between first and second icon row: `0 px`.
- First icon row content was shifted up `3 px` so the icon-row top aligns with the window top edge; second icon row and lyric area follow upward together.
- First lyric row starts at the second icon row bottom: `0 px` gap.
- Blank row between lyric rows: `56 px`.
- Gap below the second lyric row was reduced by `10 px` from the bottom edge.
- Current width target: displays about five Chinese lyric characters at `92 px` font size.
- Old full-height left mask was removed. Side mask areas are `8 px` wide on both left and right edges, starting below the `30 px` icon frame; they are currently shown with a semi-transparent gray fill for visual checking.
- Visible guide lines were removed: green outer frame, yellow toolbar frame, red lyric area frame, and blue lyric row lines are now transparent while keeping the same layout size.
- Lyric display area left, right, and bottom edges were aligned back to the main window inner edge (`0 px`) instead of extending `3 px` outside.
- Main window frame now uses a very thin `0.5 px` inner line via box-shadow, so the edge is visible without changing layout size.
- A `506 x 327 px` semi-transparent gray display frame is shown behind the full desktop lyric interface. The toolbar frame itself remains fully transparent, side mask blocks are transparent, the frame inner line is removed, and the icon 1-to-icon 2 separator is transparent.
- When the mouse is inside the current visible display frame, holding the left mouse button and dragging moves the desktop lyric window. This drag area follows the current resized display frame, not the old `506 x 327 px` area. Toolbar buttons remain clickable and are excluded from drag.
- The top and bottom edges of the lyric display area have fully transparent resize hit zones. Dragging them changes the lyric font size and the two lyric row frame heights together, from `96 px` down to about `22 px`; no visible drag bar, line, or handle is shown. When the real Electron window height changes, the lyric font size is now recalculated from that height too, so shrinking the lyric frame height also shrinks the lyric text.

## Yellow Icon Area

- Toolbar outer box: `494 x 30 px`, extended `2 px` to the left and `2 px` to the right from the previous width.
- Transparent gray icon frame length is now `506 px`, centered to match the visible main lyric frame width; icons and lyrics are unchanged.
- Toolbar top edge extension was reduced back to `0 px` so the toolbar top aligns with the window top line; the first icon row graphics were moved down `3 px` without changing icon sizes.
- The non-interactive icon frame layer at the window top edge is now fully transparent, with no border line; it does not move icons or lyrics.
- Icon row: about `446 x 24 px`.
- Single icon click box: `22 x 24 px`.
- Single visible icon: about `16 x 16 px`.
- Gap between icon boxes: about `12 px`.
- First-row icon group is left aligned inside the icon row.
- Icon group 1: leftmost first icon only, `48 px` wide with `16 px` left and `16 px` right around the icon body.
- Icon 1 uses `S` inside the circle.
- Icon 1 system tooltip text is `隐藏SHOW-0`.
- Icon 1 group, including the thin separator, was shifted `1 px` right; the `S` size was increased from `9 px` to `10 px`.
- Icon 1 graphic body was moved `2 px` right from its previous position without moving its click box or any other icon.
- A very thin vertical separator is placed between icon 1 and icon 2 and was moved `1 px` right.
- Icon 2 was restored to the hand-built SVG icon; visual size remains `17 x 18 px`, shifted `7 px` right.
- Icon 2 system tooltip text is `竖屏`.
- Icon 2 right edge to icon 3 left edge is adjusted to about `18 px` by adding `4 px` before icon 3.
- Previous icon uses a filled bar and rounded triangle, both `18 px` tall.
- Previous icon right triangle edge remains fixed; its left bar and triangle left edge extend `1 px` left.
- Previous icon was enlarged another `1 px` to the left while keeping the triangle's right edge fixed.
- Previous icon vertical bar was thickened from `3.6 px` to `4.6 px`.
- Previous icon vertical bar corners were rounded more smoothly and its width was reduced from `4.6 px` to `4.2 px`.
- Previous icon vertical bar moved `1 px` left and was reduced from `4.2 px` to `3.8 px`; triangle was not changed.
- Previous icon vertical bar was reduced from `3.8 px` to `3.4 px`; triangle right vertical edge was increased `1 px`; triangle left point was moved right to separate it from the bar.
- Previous icon triangle corners were rounded more clearly on all three points.
- Icon 3 right edge to icon 4 left edge is adjusted to about `18 px` by shifting icon 4 `6 px` left.
- Icon 4 right edge to icon 5 left edge is adjusted to match icon 2 to icon 3 by shifting icon 5 `13 px` left.
- Icon 5 right edge to icon 6 left edge is adjusted to match icon 4 to icon 5 by shifting icon 6 `15 px` left.
- Icon 5 and icon 6 were moved `2 px` right together without moving any other icons.
- Icon 6 heart click animation: shrink, grow larger than normal, then return to normal size; animation color is orange-red `#ff4f2e`.
- Icon 6 heart stroke width was increased slightly from `2.4` to `2.7`.
- Icon 6 heart body was shifted `1 px` left without moving the button.
- Icon 6 to icon 7 no longer uses extra group margin; the margin before icon 7 is `0 px`.
- Icon 1 to icon 2 visual gap is used as the current group-gap reference, about `36 px` by code-level geometry.
- Icon 6 to icon 7 visual gap is aligned to the icon 1 to icon 2 reference by shifting icon group 7-9 with transforms instead of adding extra margin.
- Icon 9 to icon 10 group gap was aligned to the icon 1 to icon 2 reference by shifting the whole icon 10-13 group together.
- The previously added icon 6-7 and icon 9-10 vertical separators were removed without moving icons or changing spacing.
- Icon 6, icon 7, and icon 8 were moved `1 px` left together.
- Icon 7 uses the referenced filled left arrow with a separate right vertical bar; it uses `currentColor`, not blue.
- Icon 8 mirrors icon 7 in the opposite direction, using a filled right arrow with a separate left vertical bar and `currentColor`.
- Icon 7 system tooltip text is `歌词后退0.5秒`; icon 8 system tooltip text is `歌词前进0.5秒`.
- Icon 7 and icon 8 arrow triangle height was increased by `2 px`, from `1.75` to `22.25`.
- Icon 7 and icon 8 rear vertical bars were increased by `2 px`, from `8.4 px` to `10.4 px`, aligned to the horizontal arrow body height.
- Icon 7 and icon 8 internal arrow parts were increased another `1 px`: triangle from `1.25` to `22.75`, rear bars to `11.4 px`.
- Icon 7 and icon 8 rear rectangles and horizontal arrow bodies were reduced by `1 px` to `10.4 px`; triangle height was unchanged.
- Icon 7 and icon 8 full internal SVG drawings were expanded from their centers by `2 px` per side, changing their visual size from `16 x 16 px` to `20 x 20 px`.
- Icon 7 and icon 8 visual width stays `20 px`; visual height was reduced again from `17 px` to `16 px`.
- Icon 7 and icon 8 were reshaped to match the user's reference: longer horizontal arrow body, rear bar aligned to the body height, mirrored directions.
- Icon 7 right edge to icon 8 left edge is adjusted to match icon 2 to icon 3 by shifting icon 8 `3 px` left.
- Icon group spacing was rechecked using icons 2-6 as the standard group; the current code-level target spacing is about `13 px` between icon graphics.
- Icon group 7-9 was aligned to that standard internally: icon 7-8 and icon 8-9 are now about `13 px`, while the icon 6-7 group gap is unchanged.
- Icon group 7-9 keeps its internal spacing after the icon 6-7 no-extra-margin correction.
- Icon 7 was moved `1 px` left by itself.
- Icon 9 was moved `2 px` left by itself.
- Icon group 10-13 was aligned to that standard internally: icon 10-11, icon 11-12, and icon 12-13 are now about `13 px`, while the icon 9-10 group gap is preserved.
- Icon 9 uses the user's reference shape directly: a large filled star with a small half-star accent at the upper left.
- Icon 9 system tooltip text is `打开滚动星图`.
- Icon 9 star was shifted `2 px` further left, from `2 px` left to `4 px` left.
- Icon 9 star visual size was reduced by `1 px`, from `20 x 20 px` to `19 x 19 px`.
- Icon 9 star group was rotated `10 deg` right from the previous angle, now `20 deg` left around its center point.
- Icon 9 default state is unselected and uses gray-white `#d8dddd`.
- All first-row icons now use the same default gray-white `#d8dddd` as icon 9.
- All first-row icons hover to a slightly whiter color `#ffffff`.
- Icon 9 star and icon 10 fish turn blue `#1aa9ff` when opened/active; other active icons keep the default gray-white unless separately animated.
- Icon row glow effects were removed from hover and active states.
- Icon 10 is forced to off-white `#f2fbfb`; icons 10, 11, 12, and 13 use a unified `15 px` right shift so the icon 9 to icon 10 visual gap matches the icon 1 to icon 2 gap.
- Icon 10 fish was replaced with the larger user reference shape: larger left tail, wide body, thicker top and bottom fins, and a dark solid circular eye; the fish head was rounded to avoid a sharp point; visual width was reduced from `22 px` to `21 px`.
- Icons 10, 11, 12, and 13 were moved `2 px` right together without moving other icons.
- Icon 11 settings gear visual size was reduced from `19 x 19 px` to `18 x 18 px`; the outer gear teeth remain `6`, with teeth at 3 o'clock and 9 o'clock and the remaining four evenly spaced.
- Icon 11 center hollow circle was enlarged again from `4.05` to `4.75` radius in the SVG viewBox.
- Icon 11 solid gear body circle was enlarged from `7.15` to `8.15` radius so the six teeth protrude less.
- Icon 11 outer teeth were widened from `5.35` to `6.35` in SVG viewBox units.
- Icon 12 lock was replaced with a custom filled-lock SVG: the lower lock body is filled and a vertical rectangular slot is cut out in the center.
- Icon 12 lock visual was shifted `1 px` left.
- Icon 12 shackle ends were extended into the lock body so there is no visible gap.
- Icons 11, 12, and 13 were moved `2 px` right together without moving icon 10 or other icons.
- Icon 13 close X visual was shifted `4.5 px` left so the lock-to-X visual gap matches the fish-to-settings visual gap, about `14.5 px`.
- Icon 13 close X was changed from Lucide default to a custom SVG; both diagonal bars now use endpoints `4.8` to `19.2` in a `24 x 24` viewBox, making the visible strokes longer than the default X.
- Icon 13 close X diagonal stroke width was increased from `2.7` to `3.1`, with round caps and round joins for smoother four endpoints.
- Next icon is the horizontal mirror of the previous icon, with matching rounded triangle, `3.4 px` rounded bar, and `18 px` height; it is shifted `13 px` left.
- Play/pause icon is shifted `5 px` left after moving icon 4 right by `1 px`.
- Play/pause icon visual size is `18 x 18 px`.
- Play/pause icon changes shape on click but does not use the blue active color.
- Pause state uses two filled bars, each `18 px` tall, with `5 px` spacing.
- Pause state bar spacing was increased by `1 px`, from `5 px` to `6 px`, by moving each bar `0.5 px` outward so icon 4's center and all other icons stay fixed.
- Pause state bars were moved `1 px` left and their corner radius was increased from `0.9 px` to `1.6 px`.
- First-row icon images are centered inside their own click boxes.

## Red Lyric Area

- Two lyric rows.
- Only two lyric rows remain; the previous separate blank spacer row was removed. The space between the two rows is now controlled by CSS `row-gap`.
- The two old full-row lyric frames were removed. Each line now keeps only its own text box, wrapping the displayed lyric text instead of the full row.
- The new lyric display box starts at `35 px` from the top of the full window and ends at about `307 px`, giving it about `272 px` height inside the `506 x 327 px` visible frame.
- Inside that lyric display box, the first lyric line is anchored to the top-left and the second lyric line is anchored to the bottom-right. The middle space is flexible and has no visible line or frame.
- The lyric display box now shrinks with the real window height. The outer page no longer forces the old full height, and the visible frame height is capped by the current window height.
- The desktop lyric window minimum height is `107 px`.
- Top row: left aligned, its row frame keeps `14 px` from both visible window left and right edges after moving the whole row frame and lyric text left `4 px`; it is visually shifted up `5 px`.
- Bottom row is right aligned, its row frame keeps `14 px` from both visible window left and right edges after moving the whole row frame and lyric text left `4 px`; it is visually shifted up `5 px`.
- Each lyric text line target height is `96 px`; each lyric frame height matches the lyric font size at `96 px`.
- Lyric font size: `96 px`.
- When the lyric area follows the height-shrink rules to its shortest state, the lyric font size target is about `22 px`.
- Font: `Microsoft YaHei UI` / `Microsoft YaHei`, Regular weight `400`.
- Sung lyric text uses a soft vertical cyan gradient per character: `0%-25%` uses very light blue `#4FFBFF`, `25%-50%` transitions to white, and the bottom `50%-100%` uses light cyan `#00FFFF`. A brightness and saturation lift is applied so the white section keeps contrast without becoming too white. Each sung glyph stroke is wrapped by a very thin black outline for shadow/depth, not by a rectangular box. Unsung lyric text remains deep blue.
- Current singing progress uses light blue fill.
- Unsung text uses deep blue base color. Its top `25%` is slightly darker, fading back into the normal deep blue by the `25%` line; its bottom `25%` is slightly lighter.
